# Отчет по анализу Poker Solver Engine

## 1. Чистое Функциональное Ядро (The Core)

### ✅ Что реализовано
*   **Pure Functions**: Движок (`src/core/engine/index.ts`) действительно построен на чистых функциях. Состояние (`GameState`) передается аргументом и возвращается новый объект. Мутации исходного состояния отсутствуют.
*   **Immutability**: Используется клонирование состояния (`cloneState`, `clonePlayers`, `clonePots`) перед внесением изменений. Это обеспечивает "слепки реальности", о которых шла речь.
*   **RNG (LCG)**: В `src/core/prng.ts` реализован линейный конгруэнтный генератор (Park-Miller). Сид сохраняется и передается, что гарантирует детерминизм и воспроизводимость.

### 👍 Что сделано хорошо
*   Архитектура позволяет легко реализовать "Time Travel" (отмену ходов) и ветвление сценариев, так как у нас всегда есть полная копия состояния на каждый момент времени.
*   Разделение логики: Правила (`rules`), Состояние (`state`), и Переходы (`engine`) четко разделены.

### ⚠️ Что требует внимания
*   **Производительность клонирования**: Сейчас используется поверхностное клонирование с ручным копированием вложенных массивов (`players.map(...)`). При симуляции миллионов рук (для AI/Солвера) это может стать узким местом. В будущем стоит рассмотреть использование библиотек типа `immer` или структур данных с sharing (persistent data structures), если профайлер покажет просадку.

---

## 2. Математический Ад (Evaluator & Showdown)

### ✅ Что реализовано
*   **Bit-Shifting Evaluator**: В `src/core/evaluator/index.ts` реализована оценка через битовые сдвиги (`(HandRank << 20) | (Kicker1 << 16)...`). Это очень эффективный способ сравнения рук.
*   **Side Pots**: Логика в `src/core/rules/index.ts` (`resolveSidePots`) и `src/core/showdown/index.ts` корректно обрабатывает создание и разрешение побочных банков.
*   **Split Pots & Odd Chips**: Реализовано честное деление банка. Лишняя фишка отдается игроку, ближайшему к слева от дилера (`getSeatDistance`), что соответствует официальным правилам.
*   **All-In Runout**: Функция `runOutBoard` в `engine/index.ts` автоматически "докручивает" борд, если игроки в олл-ине, пропуская торги.

### 👍 Что сделано хорошо
*   Эвакуатор рук выглядит компактным и быстрым.
*   Логика разделения банков (одна из самых сложных частей покера) выглядит надежной и покрывает 3-way all-in ситуации.

### ⚠️ Что требует внимания
*   **Оптимизация Эвакуатора**: Сейчас используется сортировка карт и генерация комбинаций (`getCombinations`) для 7 карт. Для high-performance солверов часто используют Lookup Tables (таблицы предрассчитанных значений), чтобы оценивать руку за O(1) без перебора комбинаций. Для текущих целей этого достаточно, но для "миллионов веток" может потребоваться оптимизация (например, Cactus Kev's algorithm).

---

## 3. Гибкость Сценариев (The Lab)

### ✅ Что реализовано
*   **Scenario Builder**: В `src/core/state.ts` (`createInitialState`) есть поддержка `ScenarioConfig`.
*   **Card Removal**: Реализовано удаление заданных карт из колоды перед шаффлом. Если мы задаем игроку "Тузов", они корректно изымаются, и никто другой их не получит.
*   **Custom Stacks & Positions**: Можно задать стеки, блайнды и рассадку.

### 👍 Что сделано хорошо
*   Это действительно превращает движок в "лабораторию". Можно воспроизвести любую раздачу из истории рук или придумать свою для тренировки.
*   Тесты в `scenarios.test.ts` подтверждают работу "God Mode" (режим бога), где мы контролируем раздачу.

### ⚠️ Что требует внимания
*   **Dealer Rotation в сценариях**: При ручной рассадке нужно внимательно следить за позицией дилера, чтобы блайнды проставились корректно. Текущая логика пытается определить SB/BB автоматически относительно дилера, что может быть неочевидно при кастомных конфигурациях (например, Dead Button).

---

## 4. Критические замечания и Риски (Critical Issues)

### 🚨 1. Целочисленная математика (Integer Math)
В текущем коде используется тип `number`, который в JS является числом с плавающей точкой.
**Риск**: Если использовать дробные блайнды (например, $0.01/$0.02), неизбежны ошибки округления (`0.1 + 0.2 !== 0.3`). Это сломает проверки равенства ставок (`bet === currentBet`) и может привести к "зависанию" игры или некорректным сайд-потам.
**Решение**: Всегда использовать **целые числа** (копейки/центы). Если блайнд $10, в конфиге должно быть `1000` (центов).

### 🚨 2. Валидация входных данных
Функция `validateAction` проверяет типы действий, но не проверяет корректность чисел.
**Риск**: Если фронтенд или бот пришлет `NaN`, `Infinity` или отрицательное число в `amount`, это значение попадет в стейт (`stack -= NaN`), что "заразит" все состояние и сделает его невалидным.
**Решение**: Добавить строгую проверку `Number.isInteger(amount) && amount >= 0` во всех методах API.

### 🚨 3. Отсутствие "Equity Calculator"
Движок умеет "доигрывать" раздачу (`runOutBoard`), но для Солвера/ИИ критически важно знать **вероятность победы (Equity)** в текущий момент, не меняя стейт.
**Риск**: Без отдельного инструмента для расчета эквити (Monte Carlo или Enumerate) невозможно построить GTO-бота или даже простой подсказчик "шансов банка". Текущая архитектура позволяет это сделать, но самого инструмента нет.

---

## Общий вердикт

**Проект находится в отличной форме.**
Фундамент (Core) заложен правильно. Самые сложные и багоопасные части (Showdown, Side Pots, RNG) реализованы на высоком уровне.

**Чего пока нет (или не замечено в явном виде):**
1.  **AI / Solver Logic**: Пока это "Движок правил" (Rules Engine). Логики принятия решений (GTO, Exploitative) или дерева поиска (CFR) пока нет. Это следующий этап.
2.  **Betting History / Hand History**: Для полноценного солвера нужно сохранять историю действий в удобном формате для анализа. Сейчас есть просто лог действий, но, возможно, потребуется более структурированный формат.

**Итог:** Это мощный бэкенд для покерного приложения или тренажера. Переход к созданию "Солвера" (ИИ) теперь возможен на этой базе.
